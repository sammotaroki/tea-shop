# Only needed when the API is hosted on a different origin than the frontend.
# Leave unset (or set to /api) when frontend and backend share the same domain.
# VITE_API_URL=https://api.yourdomain.com/api
